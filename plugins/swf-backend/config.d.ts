export interface Config {
  swf?: {
    /**
     * kogito service root URL
     * @visibility backend
     */
    baseUrl: string;
    /**
     * kogito service root URL
     * @visibility backend
     */
    port: string;
  };
}
